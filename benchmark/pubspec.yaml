name: objectbox_benchmark
description: Simple ObjectBox-Dart performance benchmark
publish_to: none

environment:
  sdk: ^3.7.0 # 3.7 has new dart format rules

dependencies:
  objectbox: any
  meta: ^1.19.0
  ffi: ^2.2.0

dev_dependencies:
  lints: ">=5.1.1 <7.0.0" # 6.0.0 requires Dart 3.8
  objectbox_generator: any
  build_runner: any
