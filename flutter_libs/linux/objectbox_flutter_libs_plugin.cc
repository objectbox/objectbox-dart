#include "include/objectbox_flutter_libs/objectbox_flutter_libs_plugin.h"

void objectbox_flutter_libs_plugin_register_with_registrar(FlPluginRegistrar* registrar) {
  // Not using method channels, so not registering one.
  (void)registrar;
}
