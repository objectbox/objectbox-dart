# Please see the documentation for all configuration options:
# https://docs.github.com/github/administering-a-repository/configuration-options-for-dependency-updates

version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    # Wait for 7 days until suggesting a new version to de-risk bugs or supply-chain attacks
    cooldown:
      default-days: 7
    # Group all GitHub Actions updates into a single merge request to simplify review
    groups:
      github-actions:
        applies-to: "version-updates"
        patterns:
          - "*"
